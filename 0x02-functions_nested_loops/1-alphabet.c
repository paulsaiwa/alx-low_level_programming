#include "main.h"
/**
 * main - unction that prints the alphabet, in lowercase,
 * Return: Always 0.
 */
int main(void)
{
	char c;
	for (c = 'a'; c <= 'z'; c++)
	{
		_putchar(c);
	}
	_putchar('\n');
	return (0);
}

